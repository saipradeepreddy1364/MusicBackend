server:
  port: ${PORT:8080}
  servlet:
    context-path: /api

spring:
  application:
    name: music-player-backend
  
  # Supabase PostgreSQL Database Configuration
  datasource:
    url: jdbc:postgresql://db.zoxztfjptjkuapqldlha.supabase.co:5432/postgres
    username: postgres
    password: Palagiri677245
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
  
  # JPA Configuration
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

jiosaavn:
  base-url: https://jiosaavn-api.pradeepreddypalagiri.workers.dev/api

logging:
  level:
    com.musicplayer: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG