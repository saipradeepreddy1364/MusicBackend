spring:
  application:
    name: music-player-backend

  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

server:
  port: ${PORT:8080}
  servlet:
    context-path: /api