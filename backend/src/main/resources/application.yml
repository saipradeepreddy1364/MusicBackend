spring:
  application:
    name: music-player-backend
  # Supabase PostgreSQL
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://db.zoxztfjptjkuapqldlha.supabase.co:5432/postgres}
    username: ${DATABASE_USER:postgres}
    password: ${DATABASE_PASSWORD:Palagiri677245}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=500,expireAfterWrite=10m
jiosaavn:
  base-url: ${JIOSAAVN_BASE_URL:https://saavn.dev/api}
server:
  port: ${PORT:8080}
  compression:
    enabled: true
logging:
  level:
    com.musicplayer: INFO
    org.springframework.web: WARN